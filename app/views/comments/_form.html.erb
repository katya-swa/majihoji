<%= form_with(model: [item, comment] ) do |form| %>
  <div>
    <%= form.text_area :content %>
  </div>
  <div class="actions">
    <%= form.submit "コメントをする", class: "btn btn-primary" %>
  </div>
<% end %>