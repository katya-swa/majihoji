<div class="container">
  <h1>ユーザー</h1>

  <div class="row">
    <ol class="users">
      <% @users.each do |user| %>
        <div class="col-xs-12 col-md-3">
          <%=user.name%>
        </div>
      <% end %>
    </ol>
  </div>

  <%= paginate @users %>
</div>